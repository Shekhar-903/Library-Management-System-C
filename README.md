# Library-Management-System-C
Library Management System in C is a console-based application that manages books using file handling. It supports adding, viewing, searching, updating, issuing, and returning books with proper input validation. The system preloads default books and ensures data persistence across program runs.
